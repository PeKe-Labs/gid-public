# Reference Implementation

GID is meant to drop into a larger system as a small, readable `.h`/`.c` — HID is
the complexity *ceiling*, not just the ancestor. A reference encoder/decoder will
live here once the wire envelope is frozen.

No runtime dependencies. Boring on purpose.
